import type { AnalyticsPageData } from "./loader";
import { AdminFilterBar } from "./dependencies";

export function AnalyticsPageSection2({ scope }: { scope: AnalyticsPageData }) {
  return (
    <AdminFilterBar />
  );
}
