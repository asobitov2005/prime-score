"use client";
import type { LoginPageClientScope } from "./controller";

export function LoginPageClientSection2({ scope }: { scope: LoginPageClientScope }) {
  return (
    {/* ════ LEFT: brand panel (lg+) ════ */}
  );
}
